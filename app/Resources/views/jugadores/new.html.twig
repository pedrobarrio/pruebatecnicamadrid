{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Jugador</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('jugadores_index') }}">Volver al Listado</a>
        </li>
    </ul>
{% endblock %}
